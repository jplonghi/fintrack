import React from 'react';

const BudgetProgress = () => {
  return (
    <div style={{
      padding: '5px',
      borderRadius: '5px',
      border: '1px solid #a970ff',
    }}>
      <h2 style={{
            color: '#ffffff',
            fontFamily: 'Space Grotesk',
            fontSize: '2rem',
            margin: '0 0 0 0',
          }}>Budget</h2>
      {/* Add budget process content here */}
      <p style={{ color: '#ffffff' }}>This section will contain the budget process functionality.</p>
    </div>
  );
};

export default BudgetProgress;
